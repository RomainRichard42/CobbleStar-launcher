# Publication du modpack

Place ici une seule version `.mrpack`, commit-la puis pousse-la sur `main`.
GitHub remplace automatiquement le pack de la release `modpack-latest`.
Le launcher compare le nom et la date de l’asset puis synchronise la nouvelle
version avant le prochain démarrage de Minecraft.
